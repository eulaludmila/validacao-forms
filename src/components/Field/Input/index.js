import styled from 'styled-components';

const Input = styled.input`

    font-size:inherit;
    padding: 10px 10px;
    /* border: solid 2px #bf1650; */
    border: 0;
    border-radius: 8px;
    box-sizing:border-box;
    transition: 0.5s;

`;

export default Input;